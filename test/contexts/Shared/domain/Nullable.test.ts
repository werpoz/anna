import { describe, it, expect } from 'bun:test';
import * as module from '@/contexts/Shared/domain/Nullable';

describe('@/contexts/Shared/domain/Nullable', () => {
  it('loads', () => {
    expect(module).toBeDefined();
  });
});
